---
title: CDA eFolder delivery
description: The end-to-end system that delivers completed CDA PDFs from loanlight-api into Encompass eFolders via EventBridge and loanlight-integrations.
icon: send
---

The CDA eFolder delivery system delivers completed collateral data audit (CDA) PDFs from `loanlight-api` into Encompass eFolders. It is split across two services: `loanlight-api` owns the **producer** (PDF identity, lender eligibility, publication outbox, and EventBridge emission), and `loanlight-integrations` owns the **consumer** (eFolder credentials, destination selection, provider delivery, and the consumer ledger).

<Warning>
**No lender is currently enabled.** No `cda_efolder_delivery_config` row is seeded. Do not enable a lender on either side until an approved provider adapter passes the provider contract suite and the per-lender preflight below is complete.
</Warning>

## How it works

When a `collateral_review_orders` row of type `cda` reaches `status = 'succeeded'` and the lender's `cda_efolder_delivery_enabled` flag is on, the order becomes **eligible**: the producer sets `cda_efolder_eligible_at` and binds the Encompass loan GUID and the PDF's SHA-256 to the order. A background sweep picks up eligible orders and publishes one EventBridge event per order to the default bus. `cda_efolder_event_published_at` is set only after EventBridge acknowledges the event — it does not mean the eFolder delivery completed.

The integrations consumer subscribes to that event type and handles eFolder credentials, destination selection, and actual delivery.

## Feature flag

`cda_efolder_delivery_enabled` is a per-lender `lender_feature_flags` row. Set it to `false` to pause new claims and new republishes. A disable is checked atomically at claim time; it does not cancel an already-acquired lease or retract an event that EventBridge has already accepted.

Keep every lender absent/off until the cross-service preflight below is complete. Set it to `true` to resume; the flag write releases only eligible rows that are unleased, already due, or carry a retry error.

<Warning>
**The only supported activation path is the authenticated internal-super-admin portal/API feature-flag route:** `PUT /api/v1/portal/lenders/:id/feature-flags`. Direct SQL (`enabled = true`) is not sufficient. The sweep checks the flag with `cdaEfolderDeliveryEnabledSql`, which validates that the flag row was written by an active internal super-admin actor on behalf of an active, non-internal lender. A row written outside this path does not satisfy those conditions and is treated as absent.
</Warning>

## Producer outbox columns on `collateral_review_orders`

Migrations `0073`–`0076` (LOA-2703) add these columns to `collateral_review_orders`:

| Column | Type | Notes |
|---|---|---|
| `cda_efolder_eligible_at` | `timestamptz` | Set when the order becomes eligible for delivery. NULL means the order is inert (pre-feature, or `cda_efolder_delivery_enabled` was off when it succeeded). |
| `cda_efolder_encompass_loan_id` | `text` | The Encompass loan GUID bound at claim time. A row cannot publish without a non-null, nonblank value. |
| `cda_efolder_pdf_sha256` | `text` | Lowercase 64-character SHA-256 of the PDF at the bound S3 key. CHECK `^[0-9a-f]{64}$`. Bound at eligibility time. |
| `cda_efolder_event_attempt_count` | `integer NOT NULL default 0` | Monotonic claim generation counter. A non-zero future `cda_efolder_event_next_attempt_at` with no error code is an active lease. |
| `cda_efolder_event_next_attempt_at` | `timestamptz` | When the next claim attempt is due. NULL after a successful publication. |
| `cda_efolder_event_published_at` | `timestamptz` | Set when EventBridge acknowledges the event. Non-null means the event was sent; it does not mean eFolder delivery completed. |
| `cda_efolder_event_last_error_code` | `text` | The most recent error, or NULL when the last attempt succeeded or no attempt has been made. |

CHECK constraints:
- `collateral_review_orders_efolder_eligibility_check` — `cda_efolder_eligible_at` is non-null only when `order_type = 'cda'`, `status = 'succeeded'`, and both `pdf_s3_key` and `cda_efolder_pdf_sha256` are non-null.
- `collateral_review_orders_efolder_publication_check` — `cda_efolder_event_published_at` is non-null only when `cda_efolder_eligible_at`, `cda_efolder_encompass_loan_id`, and `cda_efolder_event_attempt_count > 0` are all set.
- `collateral_review_orders_efolder_sha256_check` — the SHA-256 matches `^[0-9a-f]{64}$` when non-null.
- `collateral_review_orders_efolder_loan_id_check` — `cda_efolder_encompass_loan_id` is non-blank when non-null.

## The sweep

The advisory-locked sweep runs every five minutes. It picks up eligible, unleased, and due rows, acquires a generation-fenced lease, and publishes to EventBridge. The sweep emits Datadog metrics with the `environment` tag:

- `loanlight.cda_efolder.publication_claim`
- `loanlight.cda_efolder.eventbridge_accepted`
- `loanlight.cda_efolder.publication_retry`
- `loanlight.cda_efolder.expired_claim`
- `loanlight.cda_efolder.retrying_unpublished_count` — per-reason gauge of enabled, eligible, succeeded rows that are actively retrying (inside backoff or with a retry error code). Tagged by `reason` (the error code vocabulary). Emitted at the heartbeat cadence (every five minutes), not per-event.
- `loanlight.cda_efolder.sweep_heartbeat` — emitted twice per tick (once on advisory-lock acquisition, once after the batch). A tick can run for tens of minutes at the batch cap; the leading emit is what keeps the absence monitor alive.

`oldest_eligible_unpublished_age_seconds` measures time since a row became **due** (`COALESCE(next_attempt_at, eligible_at)`), not since it became eligible. A row inside its lease or backoff reads 0.

## Error codes

| Code | Meaning |
|---|---|
| `missing_encompass_loan_id` | No Encompass loan GUID is bound to the order. Reparable — see below. |
| `invalid_encompass_loan_id` | The bound GUID is not in the canonical format the contract requires. Reparable — see below. |
| `loan_lender_mismatch` | The loan's lender changed after the order was written. No repair path. |
| `invalid_pdf_s3_key` | `pdf_s3_key` diverged from the expected key. No repair path. |
| `invalid_pdf_sha256` | The stored SHA-256 does not match the PDF at the bound key. No repair path. |
| `eventbridge_rejected` | EventBridge rejected the publication call. No automatic repair. |
| `eventbridge_transport_error` | Transient network/transport error. Retries with exponential backoff capped at six hours. |

`loan_lender_mismatch`, `invalid_pdf_s3_key`, and `invalid_pdf_sha256` have no remedy inside this repository — they retry every six hours indefinitely while the `cda-efolder-backlog-aged` monitor pages. Escalate to the owning team with the order row.

## Encompass loan ID repair

When an order is parked on `missing_encompass_loan_id` or `invalid_encompass_loan_id`, an internal super-admin repair endpoint can bind or rebind the Encompass loan GUID. The repair:

- Is internal-super-admin-only. Two attestations are required: `provider_ui_verified` and `reviewed_support_case`.
- Refuses to overwrite a GUID the contract already accepts (the existing value is valid — not a repair target).
- Requires an 8–200 character printable-ASCII evidence note. Do not include borrower data, loan terms, tokens, credentials, or document contents.
- Audits both outcomes: `loan.encompass_id_linked` (first link) and `loan.encompass_id_repair_replayed` (replay of an already-correct ID). Both record `rescheduledOrderIds`.
- Wakes only rows parked on `missing_encompass_loan_id` or `invalid_encompass_loan_id`. Rows carrying `loan_lender_mismatch`, `invalid_pdf_s3_key`, `invalid_pdf_sha256`, or `eventbridge_rejected` are not touched.

<Note>
**Known window:** A row being claimed right now has a NULL error code (the claim nulls it; `scheduleRetry` re-sets it). A repair landing in this window matches nothing and audits `rescheduledOrderIds: []`. Re-run the repair — it is idempotent and audited.
</Note>

## Cross-service preflight

Before enabling a lender, verify:
- AWS account and region for the API producer.
- The EventBridge default bus and IAM permission.
- S3 bucket/key access and KMS decrypt access for the recorded PDF.
- The shared event contract release.
- The integrations consumer deployment.

The following are owned by `loanlight-integrations` and are not configurable here: target-DLQ re-PutEvents, consumer-DLQ redrive, archive replay, consumer ledger, manual-review, eFolder credentials, destination selection, and provider reconciliation.

## Consumer side (loanlight-integrations)

The integrations consumer is in `packages/encompass/src/cda-efolder-delivery/` (LOA-2847). It subscribes to `loanlight.cda.efolder.delivery.requested.v1` events from the default EventBridge bus and drives each CDA order through a durable, fenced state machine to attach the PDF to the Encompass eFolder exactly once.

### Provider modes (LOA-2921)

`CdaRuntimeConfigService` reads the deployment environment at startup and resolves a `CdaProviderMode`:

| Mode | Description |
|---|---|
| `unconfigured` | Default fail-closed adapter. No real delivery is performed; every operation rejects. |
| `developer-connect` | Encompass Developer Connect adapter. Active only on `int` and only within the demo canary scope (see below). |

The mode is determined by the `CDA_PROVIDER_MODE` environment variable. An unknown value, a missing value, or `"developer-connect"` set outside the `int` environment all cause `CdaDeploymentConfigError` at startup.

**Demo scope guard.** When `provider_mode = "developer-connect"`, four additional environment variables define the canary scope that the adapter will accept (`CDA_INT_CANARY_API_LENDER_ID`, `CDA_INT_CANARY_API_LOAN_ID`, `CDA_INT_CANARY_ENCOMPASS_LOAN_ID`, `CDA_INT_CANARY_ORDER_ID`). All four are required when in Developer Connect mode. The `CdaDemoScopeGuard` rejects any incoming CDA event whose payload does not exactly match these values — nothing outside the named canary loan can flow through the Developer Connect path in int.

The deployment config is frozen before any provider read and validated by `CdaDeploymentConfigError`-throwing checks in `CdaRuntimeConfigService`. A `contract_evidence_required` error is the only rejection reason that surfaces after startup; the others (`developer_connect_outside_int`, `demo_scope_required`, `demo_scope_partial`, `demo_scope_invalid`) fail the service before it can accept events.

### Infrastructure

| Resource | Purpose | Retention |
|---|---|---|
| Main queue `cda-efolder-delivery` | SQS worker input (300 s visibility, 5 receives) | — |
| Target-delivery DLQ `cda-efolder-delivery-target-dlq` | EventBridge failed before consumer receipt | 14 days — manual receive + exact PutEvents |
| Processing DLQ `cda-efolder-delivery-processing-dlq` | Consumer exhausted SQS receive budget | 14 days — native SQS redrive |
| Archive `cda-efolder-delivery-v1` | Durable copy of all matching source events | No expiry — replay only through a dedicated rule |
| Rule `CdaEfolderDeliveryRule*` (CDK logical ID prefix) | Exact-match EventBridge rule routing to the main queue | — |
| Ledger `cda_efolder_delivery` | Durable state, evidence, leases, generation fencing | Must outlive every replay source |
| Config `cda_efolder_delivery_config` | Per-lender destination selector and credentials | One active row per lender |

### State machine

Every `cda_efolder_delivery` row advances through a forward-only state machine keyed on `order_id`. States are ranked; a row never moves backward.

| State | Rank | Notes |
|---|---|---|
| `RECEIVED` | 0 | Event ingested, context not yet frozen |
| `SNAPSHOTTED` | 10 | Artifact verified, destination selected, context frozen |
| `ALLOCATION_INTENT` | 20 | eFolder allocation in progress |
| `MANUAL_REVIEW` | 25 | Blocked on operator action (ambiguous allocation or attribution) |
| `ATTACHMENT_READY` | 30 | Allocation confirmed, ready to upload |
| `UPLOAD_INTENT` | 40 | Upload in progress |
| `UPLOAD_RECONCILE_PENDING` | 50 | Upload response uncertain; reconciliation needed |
| `UPLOADED` | 60 | Bytes delivered to provider |
| `ASSIGNMENT_INTENT` | 70 | eFolder assignment in progress |
| `VERIFICATION_PENDING` | 80 | Assignment response uncertain; verification needed |
| `ASSIGNMENT_RECONCILE_PENDING` | 90 | Reconciliation after uncertain assignment |
| `DELIVERED` | 100 | Terminal — PDF successfully filed in the eFolder |
| `PAYLOAD_CONFLICT` | 100 | Terminal — inbound event fingerprint conflicts with a prior delivery |
| `BLOCKED` | 100 | Terminal — permanent error with no recovery path |
| `CONFLICT` | 100 | Terminal — provider state irreconcilably conflicts with ledger |

`MANUAL_REVIEW` is the only non-terminal non-runnable state — the sweep skips it until an operator closes it.

### Consumer error classes

| Class | Meaning | Recovery |
|---|---|---|
| `provider_unconfigured` | `UnconfiguredCdaProviderAdapter` active — no real adapter deployed | Deploy and configure a provider adapter |
| `configuration_missing` / `configuration_disabled` | No active `cda_efolder_delivery_config` row | Seed/enable the config row |
| `configuration_duplicate` | More than one active config row for the lender | Fix the partial unique index violation |
| `credential_reference_missing` | Secret ARN present but secret not found | Provision the secret |
| `tenant_mismatch` / `loan_mismatch` | API lender or loan does not match config | Investigate the event payload |
| `destination_zero_matches` / `destination_multiple_matches` | Destination selector matched 0 or 2+ eFolders | Verify the configured destination selector |
| `destination_inaccessible` / `destination_deleted` | eFolder no longer accessible | Do not create a replacement — escalate |
| `artifact_missing` | Source S3 object absent (bounded retry budget) | Investigate S3 lifecycle; escalate if persistent |
| `artifact_hash_mismatch` | S3 object bytes don’t match the committed SHA-256 | Terminal — escalate |
| `artifact_access_denied` / `artifact_wrong_bucket` / `artifact_wrong_prefix` | Misconfigured S3 access | Fix IAM / config |
| `artifact_unavailable` | Transient S3 error | Automatic durable backoff |
| `provider_rate_limited` / `provider_unavailable` / `provider_timeout` / `provider_transport` | Transient provider errors | Automatic durable backoff |
| `provider_conflict` | Provider reports a duplicate the ledger cannot explain | Terminal — escalate |
| `inconclusive_evidence` | Provider response lost after a side effect; evidence required | Operator must supply attribution evidence |
| `internal_error` | Unexpected engine error | Inspect logs; report |

### Manual review reasons

| Reason | When triggered |
|---|---|
| `allocation_ambiguous_without_id` | Destination matched 0 or 2+ eFolders and operator must choose one |
| `operator_attribution_unproven` | A prior attempt may have reached the provider but evidence is missing |
| `operator_closed_non_delivered` | Operator closed a non-delivered row manually |

### Consumer ledger table (`cda_efolder_delivery`)

Migration `034` (LOA-2847) in the integrations service. Key columns:

| Column group | Notable columns |
|---|---|
| **Identity** | `order_id` PK (stable idempotency key from producer), `event_bridge_event_id`, `api_loan_id`, `api_lender_id`, `encompass_loan_id`, `pdf_s3_key`, `pdf_sha256`, `filename`, `event_fingerprint` |
| **State** | `state` (forward-only, CHECK-constrained), `next_attempt_at`, `delivered_at` |
| **Frozen context** (set once, then immutable) | `resolved_lender_id`, `encompass_instance_id`, `environment`, `credential_secret_reference`, `artifact_bucket/exact_key/sha256/observed_size_bytes/etag`, `destination_selector_version`, `rendered_destination_selector`, `provider_document_id` |
| **Intent hashes** | `allocation_intent_hash`, `upload_intent_hash`, `assignment_intent_hash` — set before each provider call so a crashed attempt cannot repeat the effect |
| **Correlation IDs** | `allocation/upload/assignment_provider_correlation_id` |
| **Lease** | `lease_owner`, `lease_expires_at`, `generation` (generation-fenced — stale operators cannot overwrite newer evidence) |
| **Error** | `last_error_class`, `manual_review_reason`, `conflict_reason`, `attempts`, `artifact_missing_attempts` |

### Consumer config table (`cda_efolder_delivery_config`)

One row per lender. Partial unique index enforces at most one `active = true` row per `lender_id`.

| Column | Notes |
|---|---|
| `lender_id` | FK → `lender` |
| `active` | Must be `true` for the consumer to run against this lender |
| `encompass_instance_id` | The Encompass instance this config targets |
| `encompass_environment` | `production` or `uat` |
| `credential_secret_reference` | ARN of the Secrets Manager secret holding Encompass credentials |
| `destination_selector_version` | Schema version of the selector JSON (CHECK ≥ 1) |
| `destination_selector` | JSONB selector the consumer uses to locate the right eFolder |

### Consumer Datadog monitors

Seven monitors ship in `packages/encompass/observability/datadog/cda-efolder-delivery-*.json`:

| Monitor | Threshold |
|---|---|
| `cda-efolder-delivery-manual-review` | Alerts when a row enters `MANUAL_REVIEW` |
| `cda-efolder-delivery-oldest-unfinished-age` | Warns / alerts on stale non-terminal rows |
| `cda-efolder-delivery-payload-conflict` | Alerts on `PAYLOAD_CONFLICT` terminal state |
| `cda-efolder-delivery-processing-dlq-depth` | Alerts on processing-DLQ depth |
| `cda-efolder-delivery-sweep-heartbeat-missing` | Alerts when the sweep heartbeat is absent |
| `cda-efolder-delivery-target-dlq-depth` | Alerts on target-DLQ depth |

## Datadog monitors

Six monitors cover the CDA eFolder producer path (`packages/api/observability/datadog/`):

| Monitor | Threshold |
|---|---|
| `cda-efolder-backlog-aged` | Warns at 2 h, alerts at 6 h of oldest unpublished age. |
| `cda-efolder-publication-stuck` | Alerts when a publication claim is in-flight past a deadline. |
| `cda-efolder-sweep-missing` | Alerts after 15 minutes without a sweep heartbeat. |
| `cda-efolder-expired-claim` | Warns at ≥1, alerts at ≥3 expired leases in the last 30 minutes. An expired claim means a lease was acquired but did not finish — a flag enable or operator repair alone is not an expired claim. |
| `cda-efolder-metrics-observation-failed` | Log alert: fires when a backlog observation cycle fails (`cda_efolder.metrics_observation_failed`). While this alert is active, backlog and retry gauges may not be trustworthy — investigate database/query health before relying on them. |
| `cda-efolder-retry-backlog-stuck` | Alerts when any per-reason `retrying_unpublished_count` gauge holds ≥1 for a full 15-minute window. `published_at` means producer acceptance by EventBridge only, not completed eFolder delivery. Use the runbook SQL to find the affected order(s). |

## Querying the producer outbox

```sql
-- Inspect a specific order's producer state.
SELECT id, lender_id, loan_id, status, pdf_s3_key, cda_efolder_eligible_at,
       cda_efolder_encompass_loan_id, cda_efolder_pdf_sha256,
       cda_efolder_event_published_at, cda_efolder_event_attempt_count,
       cda_efolder_event_next_attempt_at, cda_efolder_event_last_error_code
FROM collateral_review_orders
WHERE id = :order_id;

-- List eligible, unpublished rows for a lender (enabled backlog).
SELECT orders.id, orders.lender_id, orders.loan_id,
       orders.cda_efolder_eligible_at, orders.cda_efolder_event_published_at,
       orders.cda_efolder_event_next_attempt_at,
       orders.cda_efolder_event_last_error_code
FROM collateral_review_orders AS orders
JOIN lender_feature_flags AS flags
  ON flags.lender_id = orders.lender_id
 AND flags.feature_key = 'cda_efolder_delivery_enabled'
 AND flags.enabled = TRUE
WHERE orders.order_type = 'cda'
  AND orders.status = 'succeeded'
  AND orders.cda_efolder_eligible_at IS NOT NULL
  AND orders.cda_efolder_event_published_at IS NULL
ORDER BY COALESCE(
  orders.cda_efolder_event_next_attempt_at,
  orders.cda_efolder_eligible_at
), orders.id;
```

<Note>
A historical succeeded CDA row with `cda_efolder_eligible_at IS NULL` is deliberately inert — it predates the feature or was written while the flag was off. Do not backfill it into delivery.
</Note>

## Related

<Columns cols={2}>
  <Card title="Integrations service overview" href="/integrations/service-overview" icon="layers">
    The integrations consumer that delivers to the Encompass eFolder.
  </Card>
  <Card title="Audit lifecycle" href="/integrations/audit-lifecycle" icon="workflow">
    How the AUS report delivery and findings delivery fit together.
  </Card>
  <Card title="Core tables" href="/data-model/core-tables" icon="table">
    The `loans.encompass_loan_id` column that the producer binds.
  </Card>
  <Card title="Integrations database schema" href="/integrations/database-schema" icon="database">
    The full integrations DB schema including delivery ledgers.
  </Card>
</Columns>
